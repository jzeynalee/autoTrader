# Core scientific / data stack
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.16.1

# HTTP requests and Some Tools
requests>=2.31.0
aiohttp>=3.12.15 
openpyxl>=3.1.5
pathlib>=1.0.1
setuptools>=72.1.0

# Database
sqlite-utils>=3.36.0   # helper tooling (optional, sqlite3 built-in)

# Quality checks / linting
mypy>=1.10.0
bandit>=1.7.9
ruff>=0.5.0
black>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
nose>=1.3.7

# Optional ML (if you use LSTM predictor)
scikit-learn>=1.4.0
tensorflow>=2.15.0; sys_platform != "win32" or platform_machine != "arm64"
torch>=2.2.0; extra == "ml"
hmmlearn;
xgboost;

# Visualization / reporting (optional for richer backtest reports)
matplotlib>=3.8.0
